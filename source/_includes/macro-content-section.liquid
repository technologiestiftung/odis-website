<section class="mt-10">
  <div
    class="
      {{ class }}
      bg-bg py-4 sm:py-6
      mb-0 max-w-full sm:w-screen sm:max-w-[100vw]
      sm:ml-[calc((var(--marginSize)+(var(--gridGap)*2)+var(--columnSize))*-1)]
    "
  >
    {{ content }}
  </div>
  {% if caption %}
    <figcaption
      class="
        mt-2 text-xs opacity-70 transition-opacity
        hover:opacity-100 focus-visible:opacity-100
        focusable
      "
      tabindex="0"
    >
      {{ caption }}
    </figcaption>
  {% endif %}
</section>
